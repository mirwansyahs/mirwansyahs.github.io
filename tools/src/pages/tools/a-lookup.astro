---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout
    title="A Lookup - Irwansyah Tools"
    description="Cek A record domain untuk validasi IP address tujuan menggunakan DNS over HTTPS."
    canonicalPath="/tools/a-lookup"
>
    <section
        class="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm md:p-5"
    >
        <h1 class="text-2xl font-black text-slate-900 md:text-3xl">A Lookup</h1>
        <p class="mt-1 text-sm text-slate-600">
            Lookup A record domain untuk melihat IPv4 target.
        </p>

        <div class="mt-3 grid gap-3 md:grid-cols-[minmax(0,1fr)_220px]">
            <input
                id="aDomain"
                type="text"
                class="w-full rounded-2xl border border-slate-300 p-3 font-mono text-sm"
                placeholder="contoh: example.com"
            />
            <select
                id="aProvider"
                class="rounded-2xl border border-slate-300 p-3 text-sm"
            >
                <option value="cloudflare">Cloudflare DNS</option>
                <option value="google">Google DNS</option>
            </select>
        </div>

        <div class="mt-3 flex flex-wrap gap-2">
            <button
                id="aLookupBtn"
                class="rounded-xl bg-teal-600 px-4 py-2 text-sm font-semibold text-white hover:bg-teal-700"
                >Lookup A Record</button
            >
            <button
                id="aCopyJsonBtn"
                class="rounded-xl border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-100"
                >Copy JSON</button
            >
            <button
                id="aExportTxtBtn"
                class="rounded-xl border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-100"
                >Export .txt</button
            >
        </div>
        <p id="aMessage" class="mt-3 text-sm text-slate-600"></p>
        <pre
            id="aOutput"
            class="mt-3 overflow-x-auto rounded-2xl border border-slate-200 bg-slate-50 p-3 font-mono text-xs text-slate-700">
        </pre>
    </section>

    <script>
        const domainEl = document.getElementById(
            "aDomain",
        ) as HTMLInputElement | null;
        const providerEl = document.getElementById(
            "aProvider",
        ) as HTMLSelectElement | null;
        const messageEl = document.getElementById(
            "aMessage",
        ) as HTMLParagraphElement | null;
        const outputEl = document.getElementById(
            "aOutput",
        ) as HTMLPreElement | null;
        let lastResult: unknown = null;

        const downloadTxt = (filename: string, text: string) => {
            const blob = new Blob([text], { type: "text/plain;charset=utf-8" });
            const url = URL.createObjectURL(blob);
            const link = document.createElement("a");
            link.href = url;
            link.download = filename;
            link.click();
            URL.revokeObjectURL(url);
        };

        const runLookup = async () => {
            try {
                const domain = (domainEl?.value ?? "").trim();
                if (!domain) {
                    if (messageEl)
                        messageEl.textContent = "Domain wajib diisi.";
                    return;
                }

                const provider = providerEl?.value ?? "cloudflare";
                const endpoint =
                    provider === "google"
                        ? `https://dns.google/resolve?name=${encodeURIComponent(domain)}&type=A`
                        : `https://cloudflare-dns.com/dns-query?name=${encodeURIComponent(domain)}&type=A`;

                if (messageEl) messageEl.textContent = "Memproses A lookup...";
                const res = await fetch(endpoint, {
                    headers: { Accept: "application/dns-json" },
                });
                if (!res.ok)
                    throw new Error(`DNS request gagal (${res.status})`);
                const data = await res.json();
                lastResult = data;
                const answers = (data.Answer ?? []) as Array<{
                    data?: string;
                    TTL?: number;
                }>;

                if (answers.length === 0) {
                    if (outputEl)
                        outputEl.textContent = "A record tidak ditemukan.";
                    if (messageEl)
                        messageEl.textContent =
                            "Lookup selesai. Tidak ada A record.";
                    return;
                }

                if (outputEl) {
                    outputEl.textContent = answers
                        .map(
                            (item, index) =>
                                `#${index + 1} ${item.data ?? ""} TTL=${item.TTL ?? "-"}`,
                        )
                        .join("\n");
                }
                if (messageEl)
                    messageEl.textContent = `Lookup selesai. Ditemukan ${answers.length} A record.`;
            } catch (error) {
                if (outputEl) outputEl.textContent = "";
                if (messageEl)
                    messageEl.textContent = `Gagal lookup A record: ${error instanceof Error ? error.message : "Unknown error"}`;
            }
        };

        document
            .getElementById("aLookupBtn")
            ?.addEventListener("click", runLookup);
        document
            .getElementById("aCopyJsonBtn")
            ?.addEventListener("click", async () => {
                if (!lastResult) return;
                await navigator.clipboard.writeText(
                    JSON.stringify(lastResult, null, 2),
                );
                if (messageEl)
                    messageEl.textContent =
                        "JSON hasil lookup tersalin ke clipboard.";
            });
        document
            .getElementById("aExportTxtBtn")
            ?.addEventListener("click", () => {
                const domain = (domainEl?.value ?? "lookup").trim() || "lookup";
                const output = outputEl?.textContent?.trim() ?? "";
                if (!output) return;
                downloadTxt(`a-lookup-${domain}.txt`, output);
                if (messageEl)
                    messageEl.textContent =
                        "Hasil lookup berhasil diexport ke .txt.";
            });
    </script>
</BaseLayout>
